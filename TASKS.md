# 📋 قائمة المهام والتحسينات

مهام مقترحة لتطوير المشروع، مرتبة حسب الأولوية.

---

## 🔴 أولوية عالية (Must Have)



### 2. إضافة صفحة غلاف (Cover Page)
- [x] صفحة أولى في الـ PDF فيها اسم الطفل + تاريخ + العنوان
- [x] input field في الواجهة لكتابة اسم الطفل
- [x] توليد صفحة غلاف بـ PIL (نص + إطار زخرفي بسيط)
- **الملفات المتأثرة**: `app.py` (endpoint `/pdf`), `templates/index.html`

### 3. Rate Limiting
- [x] تحديد عدد الطلبات لكل IP (مثلاً 5 كتب/ساعة)
- [x] استخدام `flask-limiter`
- [x] رسالة واضحة للمستخدم لما يوصل للحد
- **الملفات المتأثرة**: `app.py`

### 4. معالجة الأخطاء بشكل أفضل
- [x] عرض رسائل خطأ واضحة بالعربي (مش الـ raw error من الـ API)
- [x] Retry تلقائي مرة واحدة لو الطلب فشل بسبب مشكلة شبكة
- [x] Timeout واضح للمستخدم (بيتحمّل، لسة شغال، ولا فشل)
- **الملفات المتأثرة**: `app.py`, `templates/index.html`

---

## 🟡 أولوية متوسطة (Should Have)

### 5. تحسين جودة الحفاظ على شكل الطفل
- [x] تجربة رفع أكتر من صورة كـ reference (`input_image_0`, `input_image_1` — الموديل يدعم لحد 4)
- [x] تعديل البرومبت لاستخدام قوة الـ reference أكتر (مثلاً "identical face to image 0")
- [x] عمل A/B test بين برومبتات مختلفة
- **الملفات المتأثرة**: `app.py` (function `call_model` و `build_prompt`)

### 6. توليد متوازي (Parallel Generation)
- [x] بدل ما كل صفحة تتولّد ورا التانية، ولّد كل الصفحات في نفس الوقت (async)
- [x] استخدام `asyncio` + `httpx` بدل `requests`
- [x] تحسين الوقت من ~60 ثانية لكتاب 8 صفحات → ~10-15 ثانية
- **الملفات المتأثرة**: `app.py`

### 7. إعادة توليد صفحة معينة (Regenerate)
- [x] زرار على كل صفحة ناتجة "إعادة توليد" لو المستخدم مش عاجبه الشكل
- [x] Delete الملف المخزن وإعادة استدعاء الـ API
- **الملفات المتأثرة**: `templates/index.html`, `app.py`

### 8. Preview للـ PDF قبل التنزيل
- [x] عرض معاينة الكتاب كامل في modal قبل تنزيل الـ PDF
- [x] إمكانية إعادة ترتيب الصفحات (drag & drop)
- **الملفات المتأثرة**: `templates/index.html`, `app.py`

### 9. مواقف مخصصة (Custom Prompts)
- [x] كارت "موقف مخصص" يفتح input للمستخدم يكتب موقفه بنفسه بالعربي
- [x] ترجمة تلقائية للإنجليزي (استخدام Cloudflare `@cf/meta/m2m100-1.2b` مثلاً)
- **الملفات المتأثرة**: `app.py`, `templates/index.html`

### 10. حفظ الجلسة (Session Persistence)
- [x] لو المستخدم قفل التاب وفتحه تاني، يلاقي الكتاب اللي عمله
- [x] حفظ في `localStorage` للـ session_id
- [x] endpoint `/session/<id>` يرجع كل الصفحات الموجودة
- **الملفات المتأثرة**: `app.py`, `templates/index.html`

---

## 🟢 أولوية منخفضة (Nice to Have)

### 11. تنويعات في الأسلوب
- [x] اختيار سمك الخطوط (رفيع/عادي/سميك)
- [x] اختيار مستوى التفاصيل (بسيط للأطفال الصغار / تفصيلي للأكبر)
- [x] أسلوب كارتوني vs واقعي
- **الملفات المتأثرة**: `app.py`, `templates/index.html`

### 12. أرقام صفحات + هوامش للتلوين
- [x] إضافة رقم صفحة أسفل كل صفحة
- [x] هامش أبيض للطباعة
- [x] استخدام `reportlab` بدل `Pillow` للتحكم في الـ PDF
- **الملفات المتأثرة**: `app.py`

### 13. مشاركة الكتاب
- [x] زرار "شارك" يرفع الـ PDF على مساحة تخزين مؤقتة ويرجّع لينك (24 ساعة)
- [x] تخزين محلي مؤقت (بديل عملي لـ R2 بدون مفاتيح؛ جاهز للترقية لاحقًا)
- **الملفات المتأثرة**: `app.py`

### 14. Multi-language
- [x] دعم اللغة الإنجليزية للواجهة
- [x] Toggle EN/AR في الهيدر
- **الملفات المتأثرة**: `templates/index.html`

### 15. Analytics بسيطة
- [x] عدد الكتب المولّدة كل يوم
- [x] أكثر المواقف اختيارًا
- [x] استخدام JSON file بسيط أو SQLite
- **الملفات المتأثرة**: `app.py`

### 16. تنظيف الجلسات القديمة تلقائيًا
- [x] Job يمسح مجلدات `sessions` اللي أقدم من 24 ساعة
- [x] استخدام `APScheduler` أو cron خارجي
- **الملفات المتأثرة**: `app.py`

### 17. Deploy Production
- [x] ملفات النشر جاهزة: `Dockerfile` + `gunicorn_config.py` (النشر الفعلي على Fly/Railway يحتاج حسابك)
- [x] استخدام Gunicorn بدل Flask dev server
- **ملفات جديدة**: `Dockerfile`, `requirements.txt`, `gunicorn_config.py`

### 18. الدفع (Monetization)
- [x] Freemium محلي: 3 كتب مجانية/شهر لكل IP (`FREE_BOOKS_PER_MONTH`)
- [ ] Stripe للدفع الكامل (يحتاج مفاتيح حساب Stripe)
- [ ] Auth مع Google/Email (مؤجّل للمرحلة التجارية)
- **ملفات متأثرة**: `app.py`, SQLite

---

## 🐛 مشاكل معروفة (Known Issues)

- [x] خطوط إيموجي أوضح عبر CSS (`Apple/Segoe/Noto Color Emoji`) لتوحيد العرض
- [x] تصغير الصور الكبيرة على العميل قبل الرفع (max 1600px)
- [x] validation خفيف للصورة (حجم أدنى + رفض الصور الفارغة تقريبًا؛ مش face-detection كامل)

---

## 📝 نصايح للتطوير مع Cursor

- كل تاسك فيه قائمة **الملفات المتأثرة** عشان تعرف تفتحهم على طول
- ابدأ بالأحمر (Must Have) بالترتيب — تأمين التوكن هو **الأهم**
- كل تاسك تقريبًا مستقل — تقدر تشتغل عليه لوحده
- لو حبيت تجرب حاجة قبل ما تخليها permanent، اعمل branch جديد في Git

---

## 🚀 خارطة إصدارات مقترحة

- **v0.1** (الحالي): MVP شغال بالمواقف الثابتة + PDF
- **v0.2**: صفحة غلاف + Rate limiting + `.env` + معالجة أخطاء
- **v0.3**: توليد متوازي + إعادة توليد صفحة + Preview
- **v1.0**: Production deploy + Custom prompts + Analytics
- **v1.1+**: Monetization لو المشروع اتحرك تجاريًا
